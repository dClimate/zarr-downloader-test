[project]
dependencies = [
    "requests>=2.31",
    "ipykernel>=6.29.5",
]
name = "zarr-comparison-manual"
version = "0.1.0"

authors = [{ name = "0xSwego - dClimate", email = "0xSwego@gmail.com" }]
requires-python = "==3.12.*"
readme = "README.md"
license = { text = "MIT" }
